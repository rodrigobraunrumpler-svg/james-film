node_modules/
dist/
.next/
.astro/
.env
.env.*
!.env.example
!apps/api/.env.test
*.log
coverage/

# Salidas de Playwright. Se reescriben en CADA ejecución: sin estas líneas,
# correr los tests deja el árbol sucio y los informes acaban en un commit.
test-results/
playwright-report/
blob-report/

# Los scaffolds de Next y Astro crean su propio workspace: convierte la app
# en un proyecto independiente y rompe la resolución del catálogo.
apps/*/pnpm-workspace.yaml
apps/*/pnpm-lock.yaml
*.tsbuildinfo

# Vídeos de prueba del iPhone y sus conversiones: fuera del repo.
# Los .mov de apps/admin/e2e/fixtures/ SÍ van — son fixtures de 41-212 KB.
/*.MOV
/*.mov
/convertidos/
